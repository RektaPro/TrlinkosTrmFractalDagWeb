# Core dependencies
numpy>=1.20.0

# Testing
pytest>=7.0.0

# Web API (optional)
fastapi>=0.100.0
uvicorn>=0.20.0

# JIT Optimization (optional, recommended for performance)
# Provides 2-5x speedup for compute-intensive operations
# Falls back gracefully to NumPy if not installed
numba>=0.55.0

# PyTorch version (optional, for GPU acceleration)
# The core NumPy implementation works without PyTorch.
# Uncomment below for GPU support with trlinkos_trm_torch.py:
torch>=2.0.0

# LLM integration (optional)
# Uncomment for HuggingFace adapter support in trlinkos_llm_layer.py:
transformers>=4.30.0

# ONNX export (optional, for production deployment)
# Uncomment below for ONNX export functionality:
onnx>=1.12.0
onnxruntime>=1.12.0
# onnx-simplifier  # Optional: for ONNX model optimization (may have installation issues)
